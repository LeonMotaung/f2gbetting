<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Profile | Fafi2Go</title>
    <link rel="icon" type="image/png" href="/static/f2g.png">
    <link rel="manifest" href="/static/manifest.json">
    <meta name="theme-color" content="#eab308">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <link rel="icon" type="image/png" href="/static/f2g.png">
    <link rel="manifest" href="/static/manifest.json">
    <meta name="theme-color" content="#eab308">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Outfit:wght@400;700;900&display=swap');

        body {
            font-family: 'Outfit', sans-serif;
            background: #0b0f1a;
            color: white;
        }

        .menu-item {
            background: rgba(30, 41, 59, 0.3);
            border: 1px solid rgba(255, 255, 255, 0.05);
            transition: all 0.2s;
        }

        .menu-item:active {
            background: rgba(30, 41, 59, 0.6);
            transform: scale(0.98);
        }

        .verified-badge {
            background: linear-gradient(45deg, #22c55e, #16a34a);
        }
    </style>
</head>

<body class="pb-24">

    <header class="pt-12 pb-8 px-6 text-center bg-gradient-to-b from-slate-900 to-[#0b0f1a]">
        <div class="relative inline-block mb-4">
            <div
                class="w-24 h-24 bg-slate-800 rounded-full border-4 border-yellow-500 flex items-center justify-center overflow-hidden">
                <img src="<%= user.profilePicture %>" alt="Profile" class="w-full h-full object-cover">
            </div>
            <div
                class="absolute bottom-0 right-0 verified-badge w-7 h-7 rounded-full border-2 border-[#0b0f1a] flex items-center justify-center text-[10px]">
                <% if (user.isFicaVerified) { %>
                    <i class="fas fa-check text-white"></i>
                    <% } else { %>
                        <i class="fas fa-times text-white"></i>
                        <% } %>
            </div>
        </div>
        <h2 class="text-2xl font-black">
            <%= user.firstName %>
                <%= user.lastName %>
        </h2>
        <p class="text-slate-500 text-sm font-bold">
            <%= user.email %>
        </p>

        <div class="flex justify-center gap-2 mt-4">
            <% if (user.isPro) { %>
                <span
                    class="bg-yellow-500/10 text-yellow-500 text-[10px] font-black px-3 py-1 rounded-full border border-yellow-500/20 uppercase">Pro
                    Player</span>
                <% } %>
                    <% if (user.isFicaVerified) { %>
                        <span
                            class="bg-green-500/10 text-green-500 text-[10px] font-black px-3 py-1 rounded-full border border-green-500/20 uppercase">FICA
                            Verified</span>
                        <% } %>
        </div>
    </header>

    <main class="px-6 space-y-6">

        <div class="grid grid-cols-3 gap-2">
            <div class="bg-slate-900/50 p-3 rounded-2xl text-center border border-slate-800">
                <p class="text-[9px] font-bold text-slate-500 uppercase">Total Bets</p>
                <p class="text-lg font-black">
                    <%= user.totalBets %>
                </p>
            </div>
            <div class="bg-slate-900/50 p-3 rounded-2xl text-center border border-slate-800">
                <p class="text-[9px] font-bold text-slate-500 uppercase">Win Rate</p>
                <p class="text-lg font-black text-green-400">
                    <%= user.totalBets> 0 ? Math.round((user.totalWins / user.totalBets) * 100) : 0 %>%
                </p>
            </div>
            <div class="bg-slate-900/50 p-3 rounded-2xl text-center border border-slate-800">
                <p class="text-[9px] font-bold text-slate-500 uppercase">Member Since</p>
                <p class="text-lg font-black">
                    <%= new Date(user.createdAt).getFullYear() %>
                </p>
            </div>
        </div>

        <div class="space-y-3">
            <h3 class="text-xs font-bold text-slate-600 uppercase ml-2 tracking-widest">Account Settings</h3>

            <a href="/edit-profile" class="menu-item p-4 rounded-2xl flex items-center justify-between">
                <div class="flex items-center gap-4">
                    <div class="w-10 h-10 bg-blue-500/10 text-blue-500 rounded-xl flex items-center justify-center"><i
                            class="fas fa-user-edit"></i></div>
                    <span class="text-sm font-bold">Edit Profile Info</span>
                </div>
                <i class="fas fa-chevron-right text-slate-700"></i>
            </a>

            <a href="/bank-accounts" class="menu-item p-4 rounded-2xl flex items-center justify-between">
                <div class="flex items-center gap-4">
                    <div class="w-10 h-10 bg-yellow-500/10 text-yellow-500 rounded-xl flex items-center justify-center">
                        <i class="fas fa-university"></i>
                    </div>
                    <span class="text-sm font-bold">Bank Accounts</span>
                </div>
                <i class="fas fa-chevron-right text-slate-700"></i>
            </a>

            <a href="#" class="menu-item p-4 rounded-2xl flex items-center justify-between">
                <div class="flex items-center gap-4">
                    <div class="w-10 h-10 bg-purple-500/10 text-purple-500 rounded-xl flex items-center justify-center">
                        <i class="fas fa-shield-alt"></i>
                    </div>
                    <span class="text-sm font-bold">Security & 2FA</span>
                </div>
                <i class="fas fa-chevron-right text-slate-700"></i>
            </a>
        </div>

        <div class="space-y-3 pt-2">
            <h3 class="text-xs font-bold text-slate-600 uppercase ml-2 tracking-widest">Support</h3>

            <a href="/how-to-play" class="menu-item p-4 rounded-2xl flex items-center justify-between">
                <div class="flex items-center gap-4">
                    <div class="w-10 h-10 bg-green-500/10 text-green-500 rounded-xl flex items-center justify-center"><i
                            class="fas fa-question-circle"></i></div>
                    <span class="text-sm font-bold">How to Play</span>
                </div>
                <i class="fas fa-chevron-right text-slate-700"></i>
            </a>

            <button onclick="logout()"
                class="w-full bg-red-500/10 text-red-500 p-4 rounded-2xl font-bold text-sm border border-red-500/20 active:bg-red-500 active:text-white transition-all">
                LOGOUT ACCOUNT
            </button>
        </div>

    </main>

    <nav
        class="fixed bottom-0 w-full bg-slate-900/90 backdrop-blur-lg border-t border-slate-800 flex justify-around p-4">
        <a href="/game" class="text-slate-500 flex flex-col items-center gap-1"><i class="fas fa-home"></i><span
                class="text-[8px]">HOME</span></a>
        <a href="/results" class="text-slate-500 flex flex-col items-center gap-1"><i class="fas fa-chart-bar"></i><span
                class="text-[8px]">RESULTS</span></a>
        <a href="/wallet" class="text-slate-500 flex flex-col items-center gap-1"><i class="fas fa-wallet"></i><span
                class="text-[8px]">WALLET</span></a>
        <a href="/profile" class="text-yellow-500 flex flex-col items-center gap-1"><i class="fas fa-user"></i><span
                class="text-[8px]">PROFILE</span></a>

    </nav>

    <script>
        function logout() {
            if (confirm("Are you sure you want to logout?")) {
                window.location.href = "/logout";
            }
        }
    </script>
</body>

</html>