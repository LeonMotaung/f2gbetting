<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Results | Fafi2Go</title>
    <link rel="icon" type="image/png" href="/static/f2g.png">
    <link rel="manifest" href="/static/manifest.json">
    <meta name="theme-color" content="#eab308">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Outfit:wght@400;700;900&display=swap');

        body {
            font-family: 'Outfit', sans-serif;
            background: #0b0f1a;
            color: white;
        }

        .result-card {
            background: rgba(30, 41, 59, 0.5);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }
    </style>
</head>

<body class="pb-24">

    <header class="p-6 flex items-center justify-between sticky top-0 z-50 bg-slate-900/90 backdrop-blur-md">
        <h1 class="text-2xl font-black uppercase tracking-tight">Results</h1>
        <div class="bg-slate-800 px-3 py-1 rounded-full border border-slate-700">
            <span class="text-[10px] font-bold text-slate-400">TODAY</span>
        </div>
    </header>

    <main class="px-6 space-y-4">

        <% if (rounds.length===0) { %>
            <div class="text-center py-10 opacity-50">
                <i class="fas fa-history text-4xl mb-4 text-slate-600"></i>
                <p class="text-sm font-bold">No completed rounds yet.</p>
            </div>
            <% } else { %>
                <% rounds.forEach(round=> { %>
                    <div class="result-card p-4 rounded-2xl flex items-center justify-between">
                        <div class="flex items-center gap-4">
                            <div
                                class="w-12 h-12 bg-yellow-500 text-black rounded-xl flex items-center justify-center font-black text-2xl shadow-lg shadow-yellow-500/20">
                                <%= round.winningNumber %>
                            </div>
                            <div>
                                <p class="font-bold text-sm text-slate-300">Draw #<%= round.roundId.slice(-4) %>
                                </p>
                                <p class="text-[10px] text-slate-500 font-bold uppercase">
                                    <%= new Date(round.endTime).toLocaleTimeString([], {hour: '2-digit' ,
                                        minute:'2-digit'}) %>
                                </p>
                            </div>
                        </div>
                        <div class="text-right">
                            <span class="block text-[10px] font-bold text-slate-500 uppercase">Payout</span>
                            <span class="text-green-400 font-black">28x</span>
                        </div>
                    </div>
                    <% }) %>
                        <% } %>

    </main>

    <nav
        class="fixed bottom-0 w-full bg-slate-900/90 backdrop-blur-lg border-t border-slate-800 flex justify-around p-4 z-40">
        <a href="/game" class="text-slate-500 flex flex-col items-center gap-1"><i class="fas fa-home"></i><span
                class="text-[8px]">HOME</span></a>
        <a href="/results" class="text-yellow-500 flex flex-col items-center gap-1"><i
                class="fas fa-chart-bar"></i><span class="text-[8px]">RESULTS</span></a>
        <a href="/wallet" class="text-slate-500 flex flex-col items-center gap-1"><i class="fas fa-wallet"></i><span
                class="text-[8px]">WALLET</span></a>
        <a href="/profile" class="text-slate-500 flex flex-col items-center gap-1"><i class="fas fa-user"></i><span
                class="text-[8px]">PROFILE</span></a>
    </nav>
</body>

</html>